import { FilterPedidosPipe } from './filter-pedidos.pipe';

describe('FilterPedidosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPedidosPipe();
    expect(pipe).toBeTruthy();
  });
});
